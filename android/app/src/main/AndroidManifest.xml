<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- react-native-image-picker: profile photo "Take Photo" flow -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- react-native-webrtc / react-native-incall-manager (transports/webrtc) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Upshot: required from Android 13 (API 33) before any notification can be
         shown. Runtime-requested; harmless on older versions. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">

      <!-- Upshot push. The Upshot AAR ships no messaging service, so without
           this the only MESSAGING_EVENT receiver is Firebase's base class at
           priority -500, which drops data-only payloads — the form Upshot rich
           push arrives in. Priority is left at default (higher than -500) so
           this wins. -->
      <service
        android:name=".push.SwiftLoanMessagingService"
        android:exported="false">
        <intent-filter>
          <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
      </service>

      <!-- Small icon for the notification tray. Without it Android draws a grey
           square from API 21 onwards. Reusing the launcher icon for now; swap for
           a dedicated white-on-transparent silhouette before release. -->
      <meta-data
        android:name="UpshotPushSmallIcon"
        android:value="ic_launcher" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- WS3 context handoff: swiftloan://onboard?token=XXX opens the app with
             the visitor's saved journey (resolved server-side by lead token). -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="swiftloan" android:host="onboard" />
        </intent-filter>
      </activity>
    </application>
</manifest>
